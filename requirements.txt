# Shield Recall — Python dependencies
# Install with: pip install -r requirements.txt

# ── Core web framework ──────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
jinja2==3.1.4
python-multipart==0.0.9       # Required for Form() parsing in FastAPI

# ── Config management ───────────────────────────────────────────────────────
pydantic-settings==2.2.1
python-dotenv==1.0.1

# ── PII / NER detection (Presidio) ──────────────────────────────────────────
# Presidio uses spaCy under the hood for NER. The en_core_web_lg model must
# be downloaded separately after install:
#   python -m spacy download en_core_web_lg
presidio-analyzer==2.2.354
presidio-anonymizer==2.2.354
spacy==3.7.4

# ── Optional: encrypted SQLite at rest (AES-256 via SQLCipher) ──────────────
# Uncomment if you compile pysqlcipher3 against a local SQLCipher build.
# Requires: apt install libsqlcipher-dev  (Debian/Ubuntu)
#           brew install sqlcipher        (macOS)
# pysqlcipher3==1.2.0

# ── Windows-specific (automatically ignored on non-Windows) ─────────────────
# pywin32 is only needed for advanced UI Automation text extraction.
# The basic active-window title capture in os_control.py uses ctypes only.
# pywin32==306 ; sys_platform == "win32"

# ── Packaging (optional — for producing a distributable .exe) ───────────────
# pyinstaller==6.6.0
